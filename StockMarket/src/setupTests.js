// jest-dom adds custom jest matchers for asserting on DOM nodes.
// This allows you to use additional matchers for testing DOM elements in your tests.
// For example, you can check if an element has specific text content using:
// expect(element).toHaveTextContent(/react/i)
// For more information, visit: https://github.com/testing-library/jest-dom

// Importing the jest-dom library to extend Jest's assertions
import '@testing-library/jest-dom';